# Silence Agent — Donor Inflow Review

Static web app. Upload a receiving log (.xlsx/.csv); all analysis runs in the browser — no data leaves the device.

Served via GitHub Pages: Settings → Pages → Deploy from a branch → main → / (root).
